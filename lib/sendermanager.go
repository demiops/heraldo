# Sender Manager
# The sender manager takes care of loading the sender plugins, configure instances 
# Keeps track of messages queues (input/output)
# Processes the events and makes sure the input message has all the fields required
# by the event
# Instructs the different senders to relay the messages
